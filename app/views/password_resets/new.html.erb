<%= form_tag(password_resets_path, method: "post") do %>
  <%= text_field_tag :email, nil, {placeholder: I18n.translate('user.email'), class: 'form-control'} %>
  <%= submit_tag(I18n.translate('buttons.send'), {class: 'btn btn-primary'}) %>
<% end %>
